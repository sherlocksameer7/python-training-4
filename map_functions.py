num_list = [1, 56, 43, 7, 9, 8, 3]

odd_list = list(map(lambda a: a % 2 != 0, num_list))

print(odd_list)

# it checks and display all the list to show an another list creation.

num_list = [1, 56, 43, 7, 9, 8, 3]

odd_list = list(map(lambda a: a * 2, num_list))

print(odd_list)