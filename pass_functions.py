def names(name):
    for i in name:  # for loop using with printing of all values in the list.
        print(i)    # this doesn't accept the return values in the list and tuple functions.

def test(): # just some of the function creation as an empty.
    pass  # we use pass coz, we want that functions but, we don't to include any condtions or performs in it
          # so, we use pass statements to get nullifying error in it.
name_list = ["Sameer", "Dhanush", "Pradeep", "Akash"]  # new list variable creativity.

names(name_list)