num_list = [1, 50, 35, 66, 77, 25, 15, 10]

odd_list = list(map(lambda a: a ** 2, num_list))  # inorder to get all the list items to square a list in each to use map functions

print(sorted(odd_list))