num_list = [1, 56, 43, 7, 9, 8, 3]

odd_list = list(filter(lambda a: a % 2 != 0, num_list))  # it is for checking of odd numbers instead of checking with if else statements using filter functions.

print(odd_list)